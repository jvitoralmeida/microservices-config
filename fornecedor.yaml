spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
            hibernate:
                format_sql: true
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/fornecedor?autoReconnect=true&useSSL=false
    username: root
    password: admin
